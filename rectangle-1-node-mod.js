/*
	A node module that exports two properties: 
	perimeter and arear
*/
	// object property
	exports.perimeter = function (x, y) {
		return ( 2*(x+y) );
	}

	// object property
	exports.area = function (x, y) {
		return (x*y);
	}
